public class ScCalculator implements KindergartenCalculator {
    public boolean calculatePass(Score score) {
        System.out.println("SOUTH CAROLINAAA");
        return (score.getReadingScore() > 80);
    }
}
